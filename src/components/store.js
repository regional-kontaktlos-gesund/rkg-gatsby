import React from 'react'
import Order from './order'

const Store = (props) => {    
    return(
        <React.Fragment>
            <Order/>  
        </React.Fragment>
    )
}
export default Store